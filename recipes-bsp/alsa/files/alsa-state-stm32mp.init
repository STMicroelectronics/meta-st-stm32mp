#! /bin/sh

case "$1" in
start)
        /usr/bin/system-generator-alsa-states
        /usr/bin/system-generator-alsa-conf
        ;;
stop)
        ;;
*)
        echo "Usage: /etc/init.d/alsa-state-stm32mp {start}"
        exit 1
        ;;
esac
