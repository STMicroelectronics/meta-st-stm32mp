From 96e87cee6784902f44bc612243aa64a1086cba7c Mon Sep 17 00:00:00 2001
From: Christophe Priouzeau <christophe.priouzeau@foss.st.com>
Date: Fri, 31 Oct 2025 16:26:31 +0100
Subject: [PATCH] fixup-to-only-build-ns-elf-binary

Upstream-Status: Inappropriate
---
 .../StarterApp_M33TD/CMakeLists.txt           | 19 ++++++++++---------
 .../StarterApp_M33TD/CMakeLists.txt           | 19 ++++++++++---------
 .../StarterApp_M33TD/CMakeLists.txt           | 19 ++++++++++---------
 .../StarterApp_M33TD/CMakeLists.txt           | 19 ++++++++++---------
 4 files changed, 40 insertions(+), 36 deletions(-)

diff --git a/Projects/STM32MP215F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt b/Projects/STM32MP215F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt
index 0e2db835a..ecd64a7d4 100644
--- a/Projects/STM32MP215F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt
+++ b/Projects/STM32MP215F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt
@@ -178,12 +178,13 @@ add_custom_command(TARGET ${PROJECT_NAME}.elf POST_BUILD
 )
 
 # Integrate StarterApp_postbuild utility project
-set(POSTBUILD_BUILD_DIR "${CMAKE_CURRENT_BINARY_DIR}/StarterApp_postbuild")
-
-add_subdirectory(StarterApp_postbuild)
-
-add_custom_target(TARGET ${PROJECT_NAME}.elf POST_BUILD
-	COMMAND ${CMAKE_COMMAND} -S "${BASE_DIR}/StarterApp_postbuild" -G "Unix Makefiles" -B "${POSTBUILD_BUILD_DIR}"
-	DEPENDS ${PROJECT_NAME}.elf
-	COMMENT "Running post-build steps (assemble, sign, copy)"
-)
\ No newline at end of file
+#set(POSTBUILD_BUILD_DIR "${CMAKE_CURRENT_BINARY_DIR}/StarterApp_postbuild")
+#
+#add_subdirectory(StarterApp_postbuild)
+#
+#add_custom_target(TARGET ${PROJECT_NAME}.elf POST_BUILD
+#	COMMAND ${CMAKE_COMMAND} -S "${BASE_DIR}/StarterApp_postbuild" -G "Unix Makefiles" -B "${POSTBUILD_BUILD_DIR}"
+#	DEPENDS ${PROJECT_NAME}.elf
+#	COMMENT "Running post-build steps (assemble, sign, copy)"
+#)
+#
diff --git a/Projects/STM32MP235F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt b/Projects/STM32MP235F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt
index 99072c0d3..e3f04f17e 100644
--- a/Projects/STM32MP235F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt
+++ b/Projects/STM32MP235F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt
@@ -180,12 +180,13 @@ add_custom_command(TARGET ${PROJECT_NAME}.elf POST_BUILD
 )
 
 # Integrate StarterApp_postbuild utility project
-set(POSTBUILD_BUILD_DIR "${CMAKE_CURRENT_BINARY_DIR}/StarterApp_postbuild")
-
-add_subdirectory(StarterApp_postbuild)
-
-add_custom_target(TARGET ${PROJECT_NAME}.elf POST_BUILD
-	COMMAND ${CMAKE_COMMAND} -S "${BASE_DIR}/StarterApp_postbuild" -G "Unix Makefiles" -B "${POSTBUILD_BUILD_DIR}"
-	DEPENDS ${PROJECT_NAME}.elf
-	COMMENT "Running post-build steps (assemble, sign, copy)"
-)
\ No newline at end of file
+#set(POSTBUILD_BUILD_DIR "${CMAKE_CURRENT_BINARY_DIR}/StarterApp_postbuild")
+#
+#add_subdirectory(StarterApp_postbuild)
+#
+#add_custom_target(TARGET ${PROJECT_NAME}.elf POST_BUILD
+#	COMMAND ${CMAKE_COMMAND} -S "${BASE_DIR}/StarterApp_postbuild" -G "Unix Makefiles" -B "${POSTBUILD_BUILD_DIR}"
+#	DEPENDS ${PROJECT_NAME}.elf
+#	COMMENT "Running post-build steps (assemble, sign, copy)"
+#)
+#
diff --git a/Projects/STM32MP257F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt b/Projects/STM32MP257F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt
index b8e02a991..ffe8c7069 100644
--- a/Projects/STM32MP257F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt
+++ b/Projects/STM32MP257F-DK/Demonstrations/StarterApp_M33TD/CMakeLists.txt
@@ -180,12 +180,13 @@ add_custom_command(TARGET ${PROJECT_NAME}.elf POST_BUILD
 )
 
 # Integrate StarterApp_postbuild utility project
-set(POSTBUILD_BUILD_DIR "${CMAKE_CURRENT_BINARY_DIR}/StarterApp_postbuild")
-
-add_subdirectory(StarterApp_postbuild)
-
-add_custom_target(TARGET ${PROJECT_NAME}.elf POST_BUILD
-	COMMAND ${CMAKE_COMMAND} -S "${BASE_DIR}/StarterApp_postbuild" -G "Unix Makefiles" -B "${POSTBUILD_BUILD_DIR}"
-	DEPENDS ${PROJECT_NAME}.elf
-	COMMENT "Running post-build steps (assemble, sign, copy)"
-)
\ No newline at end of file
+#set(POSTBUILD_BUILD_DIR "${CMAKE_CURRENT_BINARY_DIR}/StarterApp_postbuild")
+#
+#add_subdirectory(StarterApp_postbuild)
+#
+#add_custom_target(TARGET ${PROJECT_NAME}.elf POST_BUILD
+#	COMMAND ${CMAKE_COMMAND} -S "${BASE_DIR}/StarterApp_postbuild" -G "Unix Makefiles" -B "${POSTBUILD_BUILD_DIR}"
+#	DEPENDS ${PROJECT_NAME}.elf
+#	COMMENT "Running post-build steps (assemble, sign, copy)"
+#)
+#
diff --git a/Projects/STM32MP257F-EV1/Demonstrations/StarterApp_M33TD/CMakeLists.txt b/Projects/STM32MP257F-EV1/Demonstrations/StarterApp_M33TD/CMakeLists.txt
index 54fbf3eab..11af4bfd9 100644
--- a/Projects/STM32MP257F-EV1/Demonstrations/StarterApp_M33TD/CMakeLists.txt
+++ b/Projects/STM32MP257F-EV1/Demonstrations/StarterApp_M33TD/CMakeLists.txt
@@ -181,12 +181,13 @@ add_custom_command(TARGET ${PROJECT_NAME}.elf POST_BUILD
 )
 
 # Integrate StarterApp_postbuild utility project
-set(POSTBUILD_BUILD_DIR "${CMAKE_CURRENT_BINARY_DIR}/StarterApp_postbuild")
-
-add_subdirectory(StarterApp_postbuild)
-
-add_custom_target(TARGET ${PROJECT_NAME}.elf POST_BUILD
-	COMMAND ${CMAKE_COMMAND} -S "${BASE_DIR}/StarterApp_postbuild" -G "Unix Makefiles" -B "${POSTBUILD_BUILD_DIR}"
-	DEPENDS ${PROJECT_NAME}.elf
-	COMMENT "Running post-build steps (assemble, sign, copy)"
-)
\ No newline at end of file
+#set(POSTBUILD_BUILD_DIR "${CMAKE_CURRENT_BINARY_DIR}/StarterApp_postbuild")
+#
+#add_subdirectory(StarterApp_postbuild)
+#
+#add_custom_target(TARGET ${PROJECT_NAME}.elf POST_BUILD
+#	COMMAND ${CMAKE_COMMAND} -S "${BASE_DIR}/StarterApp_postbuild" -G "Unix Makefiles" -B "${POSTBUILD_BUILD_DIR}"
+#	DEPENDS ${PROJECT_NAME}.elf
+#	COMMENT "Running post-build steps (assemble, sign, copy)"
+#)
+#
-- 
2.43.0

